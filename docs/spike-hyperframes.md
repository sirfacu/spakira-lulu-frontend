# Spike: HyperFrames ([heygen-com/hyperframes](https://github.com/heygen-com/hyperframes))

Rama: `spike/hyperframes-eval` — **PROTEGIDA** ([PROTECTED-SPIKE.md](./PROTECTED-SPIKE.md)).

## Prueba

Clone: `/facu/learning-n8n/kirajiro/hyperframes/`

```bash
cd /facu/learning-n8n/kirajiro/spakira-lulu-frontend
git checkout spike/hyperframes-eval
./scripts/hyperframes-trial.sh doctor
```

HyperFrames = HTML → MP4 (agentes). No instalar como dep del panel Vite.
Detalle: ver backend `docs/spike-hyperframes.md` en la misma rama spike.
