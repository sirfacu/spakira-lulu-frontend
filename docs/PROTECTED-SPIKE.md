# PROTEGIDA — no borrar esta rama

**Rama:** `spike/hyperframes-eval`  
**Repos:** Spa Kira frontend

## Reglas

1. **No eliminar** esta rama en local ni en GitHub.
2. **No** `git push --force` ni rebase destructivo sobre ella.
3. **No** mergear a `main` sin pedido explícito.
4. Al cambiar de tarea / volver a `main`, **preservar** esta rama.
5. Prueba HyperFrames: clone en  
   `/facu/learning-n8n/kirajiro/hyperframes/`  
   (fuera de este repo). Script: `scripts/hyperframes-trial.sh` en el backend hermano, o el de este repo.

Regla Cursor: `.cursor/rules/protected-spike-branches.mdc`.
